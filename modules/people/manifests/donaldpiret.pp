class people::donaldpiret {
  include textmate
  include onepassword

  # $home     = "/Users/${::luser}"
  # $my       = "${home}/my"
  # $dotfiles = "${my}/dotfiles"

  # repository { $dotfiles:
  #   source  => 'jbarnette/dotfiles',
  #   require => File[$my]
  # }
}